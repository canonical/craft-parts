parts:
  bash-base:
    plugin: nil
    stage-packages:
      - bash_bins
    organize:
      "*": (overlay)/
  mypart:
    after: [bash-base]
    plugin: host-overlay
    source: .
    override-overlay: |
      craftctl default
      echo "override ran" > /override-proof.txt

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "plugin_test_uv"
description = "My package description"
version = "0.0.1"

[project.scripts]
mytestuv = "mytestuv:main"
[project]
name = "plugin_test_uv"
version = "0.0.1"
description = "My package description"

[project.scripts]
mytestuv = "mytestuv:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
